version: '3.7'
services:
  python:
    container_name: python
    build:
      context: ./docker/python
    ports:
      - 8887:8888
    working_dir: /workspace
    volumes:
      - ./docker/python:/workspace
    command: >
      bash -c "jupyter notebook --no-browser --ip=0.0.0.0 --allow-root --NotebookApp.token='demo'"
